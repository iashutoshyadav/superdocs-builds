# Copy to .env and fill in. Nothing in this file is a real secret.

# Get a free key at https://use.superdocs.app -> Settings -> API Keys -> Create.
# Leave empty to run against the offline fake client (no network, no quota - what tests use by default).
SUPERDOCS_API_KEY=

PORT=8020
